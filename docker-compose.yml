version: "3.9"
services:
  api:
    build: api/.
    image: rust_microservices_api
    ports:
      - "8000:8000"
    depends_on:
      - database
  database:
    image: redis
    volumes:
      - ./data:/data
