version: "3.9"
services:
  api:
    build: api/.
    image: rust_microservices_api
    ports:
      - "8000:8000"
    depends_on:
      - database
  database:
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
